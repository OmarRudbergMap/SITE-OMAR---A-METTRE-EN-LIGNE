# Réglage Netlify — site statique (rien à compiler)
[build]
  publish = "."

# Petites règles de sécurité (facultatif mais conseillé)
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
