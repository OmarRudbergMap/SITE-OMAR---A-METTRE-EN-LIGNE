# Fichiers système
.DS_Store
Thumbs.db

# Fichiers de travail vidéo (sources non compressées)
*_src.mp4

# Le dossier vidéos EST maintenant envoyé sur GitHub (une vidéo par concert,
# total sous la limite de 1 Go de GitHub Pages). Les vidéos manquantes se
# cachent automatiquement sur le site (les photos, elles, restent).

# Dossiers techniques à ignorer
_corbeille/
omarrudberg-site/

# Remarque : supabase/config.js EST volontairement inclus.
# La clé "anon public" de Supabase est faite pour être publique
# (elle est de toute façon visible dans le site en ligne).
# La sécurité est assurée par les règles RLS dans Supabase.
